package com.example.archshowcase.core.di

import coil3.ComponentRegistry

internal actual fun addPlatformDecoders(builder: ComponentRegistry.Builder) {
    // iOS (Skia) handles animated images natively
}
